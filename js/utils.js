export function formatTimestamp(iso) { if (!iso) return '-'; const d = new Date(iso); return d.toLocaleString(); }
export function severityLabel(sev) { return {1:'高',2:'中',3:'低'}[sev] || '未知'; }
export function severityClass(sev) { return {1:'sev-high',2:'sev-med',3:'sev-low'}[sev] || ''; }
export function showModal(title, contentHtml) { let modal = document.createElement('div'); modal.className = 'modal'; modal.innerHTML = `<div class="modal-content"><h3>${title}</h3><div>${contentHtml}</div><div style="margin-top:20px"><button class="btn" onclick="this.closest('.modal').remove()">关闭</button></div></div>`; document.body.appendChild(modal); modal.addEventListener('click', (e) => { if(e.target === modal) modal.remove(); }); }
export function renderPagination(current, totalPages, onPage) { const container = document.createElement('div'); container.className = 'pagination'; for(let i=1;i<=totalPages;i++){ const btn = document.createElement('button'); btn.textContent = i; btn.className = `btn btn-sm ${i===current?'btn-primary':''}`; btn.onclick = () => onPage(i); container.appendChild(btn); } return container; }
window.utils = { formatTimestamp, severityLabel, severityClass, showModal, renderPagination };